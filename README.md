# cubbit_web backend

This repo contains the Node.js signaling + FCM backend for Cubbit.

## Folder
- `signaling-server/` : Socket.IO signaling + `/api/call-invite` + `/api/notify` FCM relay

## Deploy
Deploy `signaling-server` to Render/Railway as a Node web service.
